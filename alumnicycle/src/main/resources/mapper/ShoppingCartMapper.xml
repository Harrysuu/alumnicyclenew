<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.elec.alumnicycle.mapper.ShoppingCartMapper">

    <select id="getAllShoppingCartById" resultType="com.elec.alumnicycle.entity.ShoppingCart">
        select * from shopping_cart a where a.user_id = #{userId}
    </select>
    <select id="getSecondPostsByUserId" resultType="com.elec.alumnicycle.entity.SecondPost">

    </select>
    <select id="getAllSecondPost" resultType="com.elec.alumnicycle.entity.SecondPost">

    </select>
</mapper>